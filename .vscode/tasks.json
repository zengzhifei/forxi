{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "重启服务",
      "type": "shell",
      "command": "lsof -i :5173 | awk 'NR>1 {print $2}' | sort -u | xargs kill 2>/dev/null; sleep 1; cd ${workspaceFolder} && npm run dev",
      "problemMatcher": []
    }
  ]
}